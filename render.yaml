services:
  - type: web
    name: fullstack-app
    env: node
    plan: free
    buildCommand: |
      cd frontend && npm install && npm run build
      cd ../backend && npm install
    startCommand: node backend/app.js
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production